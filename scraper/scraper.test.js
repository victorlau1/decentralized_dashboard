import { describe, test} from "@jest"
import { NextButton, Scraper } from "./scraper"

describe("Scraper", () => {

    test("NextButton", () => {
        document = require('./ethernode.html')
        console.log(document)
    })
})
